<nav class="navbar navbar-expand-lg navbar-light bg-light py-0">
	<div class="container">
		<a class="navbar-brand" href="{{path('app')}}"><img src="{{asset('images/icon.ico')}}"></a>


		<button class="navbar-toggler fixed-top" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse " id="navbarColor03">
			<ul class="navbar-nav ms-auto">

				<li class="nav-item">
					<a class="nav-link active" href="{{path('app')}}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
			
				
				<li class="nav-item dropdown">
					 
				    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Espace RC</a>

					<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">

					{% if (app.user) and (is_granted('ROLE_RESPONSABLE')) %}
						<li><a class="dropdown-item" href="{{path('app_logout')}}">se deconnecter </a></li>
					
					{% else %}
						<li><a class="dropdown-item" href="{{path('RC_login')}}">se connecter</a></li>
						<li><a class="dropdown-item" href="{{path('register_RC')}}">S'inscrire</a></li>
						
					{% endif %}
					</ul>
					
				</li> 

				 <li class="nav-item dropdown">
				    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Espace employé</a>

					<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
					{% if (app.user) and (is_granted('ROLE_USER')) %}
						<li><a class="dropdown-item" href="{{path('app_logout')}}">se deconnecter </a></li>
					
					{% else %}
						<li><a class="dropdown-item" href="{{path('app_login')}}">se connecter</a></li>
						
						
					{% endif %}
					</ul>
					
				</li> 
			</ul>
			
			
		</div>
	</div>
</nav>
