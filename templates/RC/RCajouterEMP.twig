{% extends "RC/baseRC.html.twig" %}
                
    {% block ContentRC %}
    <style type="text/css">
        
        .container-fluid {
            background-color:rgba(10,5,3,0.1);
        }
        .btn {
            background: linear-gradient(359deg, #00ffe7 0%,#0200ff 100%);
            color:white;
        } 

        .container-fluid {
        background-color: rgb(30 137 169 / 15%);
        }

        label{
            font-weight:bold;
            color:#625a5a;
        }
        .navbar{
            position: sticky;
            top: 0;
            z-index: 100;
        }
        #wrapper{
            margin-left: auto;
            margin-right: auto;
        }
        h1{
            text-align: center;
            background: linear-gradient(359deg, #00ffe7 0%,#0200ff 100%);
            font-size: 3em;
            -webkit-background-clip :text;
            -webkit-text-fill-color:transparent;
            text-align : center;
            font-weight:bold;
        }
        (
    </style>
    <div id="wrapper">
        <div id="content-wrapper" class="d-flex flex-column">
            <div class="container-fluid  h-100 ">
                {{form_start(formEmploye)}}
                    <h1 class="h2 mb-4 text-white">Ajouter un nouveau employé</h1>
                    <div class="form-group my-1">
                    {{form_row(formEmploye.prenom)}}
                    </div>

                    <div class="form-group my-1">
                        {{form_row(formEmploye.nom)}}
                    </div>

                    <div class="form-group my-1">
                        {{form_row(formEmploye.email)}}
                    </div>

                    <div class="form-group my-1">
                        {{form_row(formEmploye.depart)}}
                    </div> 

                    <div class="form-group my-1">
                        {{form_row(formEmploye.titre_poste)}}
                    </div>

                    <div class="buttonHolder mb-5">
                        <button type="submit" class="btn btn-lg" >{{Mode}}</button>
                    </div>
                {{form_end(formEmploye)}}
            </div>
        </div>
    </div>

 <script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
<script src="vendor/jquery-easing/jquery.easing.min.js"></script>
<script src="js/sb-admin-2.min.js"></script> 
    {% endblock %}